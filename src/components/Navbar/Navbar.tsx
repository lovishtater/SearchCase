import Logo from "../../logo.svg";
import "./navbar.css";

const Navbar = () => {
    return (
        <div className="navbar">
                <img src={Logo} alt="logo" />
        </div>
    );
};

export default Navbar;
